---

# Removed the following until resolved issues installing:
#       - winehq-stable

tools:
  packages:
    install:    
      - code
      - virtualbox-5.1
      - terminator
      - openjdk-11-jdk
      - gimp
      - inkscape
      - maven
      - eclipse
      - google-chrome-stable